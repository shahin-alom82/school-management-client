import React from 'react';
import TopHeader from './TopHeader';

const Header = () => {
      return (
            <div>
                  <TopHeader />
            </div>
      );
};

export default Header;